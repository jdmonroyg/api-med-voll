spring.profiles.active=dev, test, prod
spring.devtools.add-properties=false
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost/vollmed_api
spring.datasource.username=${DB_USERNAME}
#importado desde ambiente intellij
spring.datasource.password=${DB_PASSWORD}

#Show sql and format
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#para evitar que se imprima la pila de errores
server.error.include-stacktrace=never

# sirven para cambiar de nombre a los atributos de paginacion y ordenamiento
#spring.data.web.pageable.page-parameter=pagina
#spring.data.web.pageable.size-parameter=tamano
#spring.data.web.sort.sort-parameter=orden
#importado desde ambiente intellij
api.security.secret=${JWT_SECRET}